# Estado al 05/08/2026 · laOra

Notas de trabajo para retomar sin releer la conversación entera.

## Lo que está en producción

| Dirección | Qué es |
|---|---|
| `laora.es` | **Portada nueva** (el material aprobado del 05/08). La anterior sigue generándose en `/home-anterior` |
| `laora.es/lunarv2c` | **Pantalla individual del Lunar**: configurador de acabado y correa, precio siempre visible y ficha técnica en pantalla nueva. Óscar la dio por buena: las fichas del resto de relojes serán como esta |
| `laora.es/carrito` | La cesta con lo elegido. Pagar abre PayPal (`paypal.me/saneascom`) con el importe |
| `laora.es/cuenta` | Entrar con el correo, sin contraseña. **Funcionando y probado de extremo a extremo** |
| `laora.es/lunarv2` | Salta a la portada (301) |

## Los generadores

Las páginas NO se editan a mano. Cada una tiene el suyo:

- `generar.py` → las 4 de sección, las 8 fichas y `home-anterior.html`
- `generar_v2.py` → `index.html`, la portada
- `generar_v2c.py` → `lunarv2c.html`, el configurador. `SLUG = 'lunar'`: para el
  siguiente reloj se cambia esa línea y su tabla de fotos
- `generar_carrito.py` → `carrito.html`
- `generar_cuenta.py` → `cuenta.html`

Cada uno lleva su `V_CSS` y su `V_JS`: **subirlos en cada cambio** o Cloudflare
sirve la versión vieja hasta cuatro horas. Lo mismo vale para las IMÁGENES, que
no llevan versión: si se sustituye una foto, hay que **cambiarle el nombre**.
Costó dos vueltas descubrirlo con la foto del Cóctel.

## La cuenta · dónde está cada cosa

- Proyecto de Supabase: **el de Activala**, `uikanfvigunjhzibnhxf`, compartido.
  Decisión de Óscar sabiendo que los usuarios de los dos negocios van a la misma
  lista y que el remitente de correo también es común.
- Claves públicas ya puestas en `assets/js/cuenta.js` (la *publishable* del
  formato nuevo; el endpoint `/auth/v1/otp` la acepta en la cabecera `apikey`).
- `https://laora.es/**` añadido a Redirect URLs. Sin eso el enlace volvía a
  `localhost:3000`.

## Lo siguiente

1. **Las plantillas de correo** (Authentication → Emails). Llegan en inglés y
   firmadas por «Supabase Auth». Hay que reescribirlas en español. OJO: las
   plantillas son del PROYECTO, así que las hereda Activala. Redactar neutro y
   NO firmarlas como laOra.
2. **SMTP propio.** El envío integrado de Supabase es de desarrollo y va limitado
   a unos pocos correos por hora **para todo el proyecto**. Con eso no se puede
   vender. Con Resend: host `smtp.resend.com`, puerto 465, usuario `resend`, la
   contraseña es la API key —la pone Óscar— y el *sender name* «Grupo Saneas».
   Es lo que además permite quitar el «Opt out of these emails» del pie, que en
   un correo de acceso es una trampa: quien lo pulse deja de poder entrar.
3. **La pantalla del socio**: sus relojes, la factura, la garantía y el historial
   del taller. Hoy entrar solo confirma que has entrado.
4. **La cesta entre aparatos.** Vive en el navegador donde se creó. Con la cuenta
   en marcha ya se puede hacer que siga al cliente al móvil.
5. **Datos de envío.** No se piden en ninguna pantalla: hoy no se puede enviar
   nada a nadie.
6. **Tarjeta con Mollie.** La clave está puesta y es **`live_`**: la primera
   prueba cobra de verdad. `crear-reserva.ts` está en la etiqueta
   `web-v1-antes-del-rediseno`, pero calcula precios leyendo `precios.js`, que ya
   no existe: hay que apuntarla a `catalogo.json`.

## Avisado a Óscar y sin resolver

- Las **ocho fotos por combinación** del configurador. Las hace ChatGPT.
- El **aviso de marcas a 9 px**, por debajo de su propio suelo de 12.
- **«Garantía hasta 5 años»**: si depende de algo (registro, revisiones), conviene
  escribirlo antes de la primera venta.
- La ficha del Lunar dice **zafiro y brazalete 904L**; la hoja da mineral
  endurecido y 316L en el acabado de entrada.
- El diálogo dice **«mecanismos suizos y japoneses»**: cierto en la colección,
  pero el Lunar no lleva ninguno suizo (VK63 japonés y ST19 chino).
- Desde la portada **ya no se llega a la colección** salvo por el menú.
